management:
  endpoints:
    web:
      exposure:
        include: prometheus
  endpoint:
    prometheus:
      enabled: true
  server:
    port: 8091
  tracing:
    sampling:
      probability: 1.0

spring:
  security:
    user:
      name: prometheus
      password: secret123
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8080/realms/sep-realm
          jwk-set-uri: http://localhost:8080/realms/sep-realm/protocol/openid-connect/certs

keycloak:
  realm: sep-realm
  auth-server-url: http://localhost:8080
  resource: sep-bank1-backend
  credentials:
    secret: r8Mh0Jq59HkhLr2GC8NNjx6twBOsDhFj

tracing:
  url: ${TRACING_URL:http://localhost:4318/v1/traces}

logging:
  pattern:
    level: "%5p [${spring.application.name:},%X{traceId:-N/A},%X{spanId:-N/A}]"
  file:
    name: F:\\Nevena\\faks\\master\\SEP\\projekat\\sep\\monitoring\\logs\\bank1.log